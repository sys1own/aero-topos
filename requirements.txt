# ==============================================================================
# Aero Topos Phase IV -- Core Runtime Dependencies
# ==============================================================================

# --- Syntax Parsing & Structural AST Layers ---
# tree-sitter powers cross-language AST parsing (Rust/C/C++/Fortran).
tree-sitter>=0.25,<0.26
tree-sitter-python>=0.23,<0.25
tree-sitter-rust>=0.24,<0.25
tree-sitter-c>=0.24,<0.25
tree-sitter-cpp>=0.23,<0.24
tree-sitter-fortran>=0.5,<0.6
PyYAML>=6.0,<7.0

# --- High-Performance Hardware-Accelerated Math Substrate ---
# Upgraded to NumPy 2.x and JAX/XLA/Triton to back parallelized 
# Geometry of Interaction matrix solvers and non-commutative quantale joins.
numpy>=2.0.0
jax>=0.4.30
jaxlib>=0.4.30
diffrax>=0.5.0
triton>=3.0.0
scipy>=1.12.0

# --- Semantic Topologies & Verification ---
# networkx handles UAST semantic mapping graph definitions.
# z3-solver backs the Precision Shield SMT validation layer.
networkx>=3.1,<4.0
z3-solver>=4.12,<5.0

# --- Optional: Distributed Build Backends ---
# Lazy-imported. If absent, the engine falls back to local execution.
fabric>=3.0,<4.0
kubernetes>=29.0,<32.0
